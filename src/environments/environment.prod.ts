export const environment = {
  production: true,
  appName: 'Aviation ERP',
  // TODO: point this at the deployed .NET Web API before shipping.
  apiUrl: '/api',
  // TODO: flip to false once the Oracle-backed .NET Web API is live.
  useMockApi: true,
  tokenStorageKey: 'aviation_erp_token',
  mockLatencyMs: 0
};
